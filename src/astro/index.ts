// Auto-merged into <PortableText> on the site at build time. Don't import this
// file in your Astro pages directly — EmDash wires it up automatically.
import ContactForm from "./ContactForm.astro";

export const blockComponents = {
  contactForm: ContactForm,
};
